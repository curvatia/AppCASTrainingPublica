<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AppCASTrainingPublica" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Diseño botón inicio sesión con usuario/constraseña-->
    <style name="btnApp" >
        <item name="android:background">@drawable/background_btn</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:paddingStart">10dp</item>
        <item name="android:paddingEnd">10dp</item>
    </style>

    <!-- Diseño editText ExpanableListView -->
    <style name="expanableStyle">
        <item name="android:background">@drawable/background_expanable</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:paddingStart">30dp</item>
    </style>
    <!-- Diseño ExpanableListView view child-->
    <style name="expanableStyleChild">
        <item name="android:background">@drawable/background_expanable</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:paddingStart">30dp</item>
    </style>



</resources>